name: aplikasi_kasir
description: Aplikasi Kasir Digital KasirKu

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  supabase_flutter: ^2.3.0
  excel: ^4.0.2
  path_provider: ^2.1.1
  share_plus: ^7.2.1
  intl: ^0.19.0
  uuid: ^4.3.3
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1
  fl_chart: ^1.2.0
  file_saver: ^0.2.5 # [TAMBAHAN] Untuk auto-download file Excel

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true